import './PageNotfound.scss';

export const PageNotfound = () => {
  return (
    <div
      className="not-found"
      style={{ backgroundImage: 'url(img/page-not-found.png)' }}
    ></div>
  );
};
